# CommunityHub Admin

Administrative interface for platform and community administrators.

## Access model
- Super Admin: platform-wide operations.
- Community Admin: assigned community only.

The UI must never be treated as the authorization boundary; the API enforces all permissions and tenant isolation.
